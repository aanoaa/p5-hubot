### perl5-hubot ###

    $ git clone git://github.com/aanoaa/p5-hubot.git
    $ cd p5-hubot/
    $ cpanm --installdeps .    # fail? add `--no-test` option and again
    $ perl -Ilib bin/hubot
    hubot> exit    # leave
